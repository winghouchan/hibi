name: Onboarding > when complete > and the app is opened in the note editor > the home screen is shown
appId: ${appId}
---
- runFlow:
    file: ../../../fixtures/loadDatabaseFixture.yaml
    env:
      databaseFixture: onboarding/complete

- openLink:
    link: hibi://onboarding/notes/edit/1

- assertVisible:
    id: home.screen
