name: Library > when a note exists > it can be navigated to via a deep link
appId: ${appId}
---
- runFlow:
    file: ../../../fixtures/loadDatabaseFixture.yaml
    env:
      databaseFixture: onboarding/complete

- openLink:
    link: hibi://note/1

- assertVisible:
    id: library.note.screen
