name: Library > when onboarding has not been completed > the user is redirected to the welcome screen
appId: ${appId}
---
- clearState

- openLink:
    link: hibi://library

- assertVisible:
    id: onboarding.welcome.screen
