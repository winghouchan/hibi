name: Launch app
appId: ${appId}
tags:
  - subflow
---
- runFlow:
    when:
      true: ${loadBundle}
    file: ../../bundle/loadBundle.yaml

- openLink:
    link: ${link}

- runFlow:
    file: ./verify-app-open.yaml
