name: Launch app
appId: ${appId}
tags:
  - subflow
---
- runFlow:
    when:
      true: ${clearState}
    commands:
      - clearState

- runFlow:
    when:
      true: ${loadBundle}
    file: ../../bundle/loadBundle.yaml

- launchApp

- runFlow:
    file: ./verify-app-open.yaml
